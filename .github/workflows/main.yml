- name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # Use '18', '20', or '22' depending on your project
          cache: 'npm'       # Automatically caches your node_modules
